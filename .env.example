# ── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql+asyncpg://finbot:finbot@localhost:5433/finbot
# Required by docker-compose.prod.yml for the PostgreSQL container
POSTGRES_PASSWORD=changeme

# ── Telegram ──────────────────────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=your-bot-token-from-botfather
# Comma-separated list of Telegram user IDs allowed to use the bot
ALLOWED_TELEGRAM_USER_IDS=[123456789,987654321]

# ── Local LLM (Ollama) ───────────────────────────────────────────────────────
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b-instruct-q4_K_M

# ── Fallback LLM (paid API) ──────────────────────────────────────────────────
# Provider: "anthropic" or "openai"
FALLBACK_LLM_PROVIDER=anthropic
FALLBACK_LLM_MODEL=claude-3-5-haiku-latest
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# ── Categories ────────────────────────────────────────────────────────────────
# Comma-separated default expense categories (seeded into DB on first boot)
# DEFAULT_CATEGORIES=clothing,coffee,dining,education,entertainment,gas,gifts,groceries,health,home,insurance,personal,subscriptions,transport,travel,utilities

# ── Mini App ─────────────────────────────────────────────────────────────────
# Base URL for the Telegram Mini App (hosted on GitHub Pages or similar).
# Leave empty to disable the /add command.
# WEBAPP_BASE_URL=https://<username>.github.io/finbot/

# Port for the Mini App API server (default 9876).
WEBAPP_PORT=9876

# ── Tunnel ───────────────────────────────────────────────────────────────────
# Auto-start a localtunnel when the bot boots to expose the local API server
# via a public HTTPS URL. Requires `lt` (npm install -g localtunnel) or `npx`.
# The tunnel URL is auto-detected and passed to the Mini App as &api=.
TUNNEL_ENABLED=false
# Request a specific subdomain for a stable URL (optional, not guaranteed).
# TUNNEL_SUBDOMAIN=

# ── General ───────────────────────────────────────────────────────────────────
DEFAULT_CURRENCY=ILS
ASSUME_HALF_SPLIT=false
DEBUG=false
